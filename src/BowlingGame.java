class BowlingGame {
    BowlingGame game = new BowlingGame();

    void roll(int pins) {
    }

    int score() {
        return 0;
    }

    void repeat() {
        int pins = 0;
        game.roll(pins);
    }
    int score = game.score();
}
